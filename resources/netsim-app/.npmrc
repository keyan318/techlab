# This project lives on an ExFAT volume with no symlink support,
# so npm must not create node_modules/.bin symlinks. Package scripts
# invoke tools via `node node_modules/<pkg>/...` directly.
bin-links=false
